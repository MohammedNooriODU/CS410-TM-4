/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  //   distDir: "dist",
  basePath: "/CS410-TM-4",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
